Finance app sandbox! 

the .NET version tests ASP.NET Web Api and EF Core. There is also a frontend with React and MUI.

the Python version uses FastAPI and SQLAlchemy. There is also a frontend with Svelte and ShadCN
